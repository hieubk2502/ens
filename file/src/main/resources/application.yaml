spring:
  application:
    name: file
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/ens}
    username: ${DB_USERNAME:ens-user}
    password: ${DB_PASSWORD:Aa123456@}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:none}
    show-sql: ${JPA_SHOW_SQL:true}
    properties:
      hibernate:
        format_sql: true
        default_schema: ${DB_SCHEMA:hrm}
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    serialization:
      fail-on-empty-beans: false
minio:
  url: ${MINIO_URL:https://dev-minio-oneauto.tasco.com.vn}
  access-key: ${MINIO_ACCESS_KEY:GnCcoYiW1AIc747fatY1}
  secret-key: ${MINIO_SECRET_KEY:U0Oq85162QrANZatX0AM1AXydcrVpBoLr1QJxHul}
  bucket: bucket-hieu-test